# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a version bump for `@mdkit/core` and
`@mdkit/cli`, then `pnpm version-packages` and `pnpm release` to
publish.
